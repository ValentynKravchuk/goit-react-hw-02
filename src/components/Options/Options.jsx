import React from "react";

function Options(value) {
  return (
    <div>
      <button>Good</button>
      <button>Neutral</button>
      <button>Bad</button>
      <button>Reset</button>
    </div>
  );
}

export default Options;
